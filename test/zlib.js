const assert = require('assert');

/*describe('zlib', () => {
  var zlib = require('../zlib');

  it('.gzip().then().gunzip()', done => {
    zlib.gzip('test').then(res => {
      return zlib.gunzip(res);
    }).then(str => {
      assert.equal(str, 'test');
      done();
    });
  });

  describe('callback support', () => {
    it('.gzip() and .gunzip()', done => {
      zlib.gzip('test', (err, res) => {
        assert(!err);
        assert(Buffer.isBuffer(res));
        zlib.gunzip(res, (err, string) => {
          assert(!err);
          assert.equal(string, 'test');
          done();
        });
      });
    });
  });
});*/
