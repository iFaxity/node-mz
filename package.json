{
  "name": "@ifaxity/mz",
  "version": "1.0.2",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "mocha --reporter progress",
    "coverage": "nyc --reporter=lcov --reporter=text npm run test"
  },
  "engines": {
    "node": ">=8"
  },
  "files": [
    "*.js"
  ],
  "devDependencies": {
    "mocha": "^5.2.0",
    "nyc": "^13.1.0"
  }
}
