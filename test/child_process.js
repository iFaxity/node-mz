const assert = require('assert');

/*describe('child_process', () => {
  const child = require('../child_process');
  const { version } = process;
  const parseVersion = stdout => stdout.toString('utf8').substr(0, version.length);

  it('exec().then()', done => {
    child.exec('node --version').then(stdout => {
      assert.equal(version, parseVersion(stdout));
      done();
    });
  });

  it('exec().catch()', done => {
    child.exec('__mztestnotarealcommand__').catch(done);
  });

  describe('callback support', () => {
    it('exec() success', done => {
      child.exec('node --version', (_, stdout) => {
        assert.equal(version, parseVersion(stdout));
        done();
      });
    });

    it('exec() err', done => {
      child.exec('lkajsdfkljalskdfjalsdf', err => {
        assert(err);
        done();
      });
    });
  });
});*/
